
// Дата
XmlЗначение(Тип("Дата"), "2015-08-01T22:22:22")

//Обход ТЧ с одной строкой и несколькими
    ИспользуемыеМатериалы = СтруктураПреобразованияРезультата.ИспользуемыеМатериалы;
	Заказы = СтруктураПреобразованияРезультата.ЗаказыНаПроизводство;
	СтруктураПреобразованияРезультата.Удалить("ИспользуемыеМатериалы");
	СтруктураПреобразованияРезультата.Удалить("ЗаказыНаПроизводство");
	
	Если ТипЗнч(ИспользуемыеМатериалы.Строки) = Тип("СписокXDTO") Тогда
		Для Каждого СтрИспользуемыеМатериалы Из ИспользуемыеМатериалы.Строки Цикл
			НовСтр = ЗагружаемыйОбъект.Материалы.Добавить();
			Номенклатура = Справочники.Номенклатура.ПолучитьСсылку(Новый УникальныйИдентификатор(СтрИспользуемыеМатериалы.Номенклатура));	
			НовСтр.Номенклатура = Номенклатура;	
			//НоваяСтрокаСостав.ЕдиницаИзмерения = Справочники.УпаковкиЕдиницыИзмерения.ПолучитьСсылку(Новый УникальныйИдентификатор(СтрокаТаблицаИспользуемыеМатериалы.ЕдиницаИзмерения));
			НовСтр.ЕдиницаИзмерения = Номенклатура.ЕдиницаИзмерения;
			НовСтр.ЦехПолучатель = Справочники.СтруктураПредприятия.ПолучитьСсылку(Новый УникальныйИдентификатор(СтрИспользуемыеМатериалы.ЦехПолучатель));
			НовСтр.Количество = СтрИспользуемыеМатериалы.Количество;
			НовСтр.ГруппаМастера = Справочники.Бригады.НайтиПоНаименованию(СтрИспользуемыеМатериалы.ГруппаМастера);
		КонецЦикла;
	ИначеЕсли ТипЗнч(ИспользуемыеМатериалы.Строки) = Тип("ОбъектXDTO") Тогда   
		НовСтр = ЗагружаемыйОбъект.ИспользуемыеМатериалы.Добавить();
		Номенклатура = Справочники.Номенклатура.ПолучитьСсылку(Новый УникальныйИдентификатор(ИспользуемыеМатериалы.Строки.Номенклатура));
		НовСтр.Номенклатура = Номенклатура;
		//НоваяСтрокаСостав.ЕдиницаИзмерения = Справочники.УпаковкиЕдиницыИзмерения.ПолучитьСсылку(Новый УникальныйИдентификатор(ТаблицаИспользуемыеМатериалы.Строки.ЕдиницаИзмерения));
		НовСтр.ЕдиницаИзмерения = Номенклатура.ЕдиницаИзмерения;
		НовСтр.ЦехПолучатель = Справочники.СтруктураПредприятия.ПолучитьСсылку(Новый УникальныйИдентификатор(ТаблицаИспользуемыеМатериалы.Строки.ЦехПолучатель));
		НовСтр.Количество = ТаблицаИспользуемыеМатериалы.Строки.Количество;
		НовСтр.ГруппаМастера = Справочники.Бригады.НайтиПоНаименованию(ТаблицаИспользуемыеМатериалы.Строки.ГруппаМастера);
	КонецЕсли;

